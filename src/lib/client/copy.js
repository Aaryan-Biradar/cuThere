/**
 * Small shared UI copy / fallback constants reused across pages and cards.
 * Intentionally lightweight — not an i18n system, just a single source of truth
 * for a few strings that were previously duplicated inline.
 */

/** Fallback title when an event has no usable title. */
export const UNTITLED_EVENT = 'Untitled Event';

/** Default hero / detail image when an event has no image of its own. */
export const DEFAULT_EVENT_IMAGE = '/heroimage.png';

/** Fallback location shown on event cards when none is provided. */
export const DEFAULT_EVENT_LOCATION = 'Carleton University Campus';
